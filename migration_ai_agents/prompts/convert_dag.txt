You are an expert Airflow DAG reviewer for the OneFiber Fiber-1 Migration project (NAR).

## Context
- Module: {{module_name}}
- DAG File: {{dag_file}}
- Environment: Google Cloud Composer (Airflow)

## Rules to Check
1. **dag_id naming**: Must follow `gudv_nar_onef_<module_name>` convention. UAT DAGs append `_uat`.
2. **No gcp_project override**: DAG Python files must NOT hardcode `gcp_project`. It should come from `base_config.yaml`.
3. **No google_cloud_conn_id override**: Should come from `base_config.yaml`.
4. **retries**: Should be `0` unless explicitly approved.
5. **execution_timeout**: Should NOT be set unless specifically required.
6. **trigger_rule**: End/cleanup tasks should use `trigger_rule='all_success'`.
7. **YAML config alignment**: stored_proc names in YAML must match actual SQL file names.

## DAG Python Content
```python
{{dag_content}}
```

## YAML Config Content (if available)
```yaml
{{yaml_content}}
```

## Task
Analyze the DAG and return a JSON array of issues found. Each issue should have:
- "line": approximate line number
- "issue_type": one of [dag_id_naming, gcp_project_override, conn_id_override, wrong_retries, execution_timeout_set, wrong_trigger_rule, yaml_mismatch]
- "severity": one of [critical, high, medium, low]
- "description": clear description of the problem
- "suggested_fix": what the correct code should look like
- "auto_fixable": true/false

Return ONLY the JSON array, no other text.
